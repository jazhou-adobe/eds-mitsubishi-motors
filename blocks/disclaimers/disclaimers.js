export default async function decorate(block) {
  // Static legal/disclaimer prose — no dynamic behavior required.
  // The block exists only to receive the light-grey section background
  // and disclaimer typography via disclaimers.css.
}
