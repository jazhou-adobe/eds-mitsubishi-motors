export default async function decorate(block) {
  // Hero block: first cell = background image, second cell = text content
  // No additional JS restructuring needed — CSS handles layout via
  // absolute positioning of the image cell behind the text cell.
}
